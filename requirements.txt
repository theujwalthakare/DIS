psutil>=5.9
prometheus-client>=0.14
kubernetes>=24.0.0
scikit-learn>=1.2
joblib>=1.2
pyyaml>=6.0
numpy>=1.21
pandas>=1.3
matplotlib>=3.5
scipy>=1.7

# Optional: For real Prometheus metrics collection
# prometheus-api-client>=0.5

# Note: TensorFlow is optional and may not be available for all Python versions
# Install TensorFlow separately if you need the autoencoder trainer:
# python -m pip install tensorflow
