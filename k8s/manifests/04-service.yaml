apiVersion: v1
kind: Service
metadata:
  name: dis-metrics
  namespace: dis-system
  labels:
    app: dis-agent
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: metrics
  selector:
    app: dis-agent
