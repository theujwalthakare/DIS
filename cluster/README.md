Cluster manifests and monitoring configuration.

Edit `agent-daemonset.yaml` to set image/name and RBAC as needed for your cluster.

Prometheus config here is an example `prometheus.yml` for scraping aDC agents.
